#include<iostream>
#include<cmath>
#include<vector>
#include<queue>
#include<stack>
using namespace std;

// Types of Inheritance
// Single inheritance : When class A inherits class B only one level
// Multilevel inheritance : e.g. A is parent of B and B is parent of C

// Heirarchial inheritance : e.g. Vehicle class can have multiple children like 'car', 'bike', 'cycle', etc...

// Multiple inheritance : When a class inherits properties from multiple classes...
//                        e.g. class "Son" is inheriting properties from two classes "mother" and "father"
                        
                    // class son: public father, protected mother{
                    // }

                    // Here, when we create an object of "son" class,,, then it will first call the default constructor of "father" class & then of "mother" class, bcoz father is mentioned first :)


int main(){

    

    return 0;
}

