#include<stdio.h>

int main()
{
    int start, step, end;
    printf("Enter start value : ");
    scanf("%d", &start);
    printf("Enter step value : ");
    scanf("%d", &step);


    printf("The values of start& step are %d and %d respectively, Sum of start step is : %d I guess ", start, step, start+step);


    return 0;
}
