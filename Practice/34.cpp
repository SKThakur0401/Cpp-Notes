#include<iostream>
#include<cmath>
#include<vector>
#include<queue>
#include<stack>
using namespace std;

class trio{

    int quan;
    int cost;
    double ratio;

    trio(int quan, int cost, int ratio)
    {
        this->quan=quan
        this->cost=cost
        this->ratio=ratio
    }
}

fun compare(trio a, trio b)
{
    return a.ratio> b.ratio
}

int solve(int budget, vector<int> bundleQuantity, vector<int> bundleCosts)
{
    
}


int main(){

    solve(budget, bundleQuantity, bundleCosts);
    
    return 0;
}

